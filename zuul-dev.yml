eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
zuul:
  routes:
    client:
      path: /**
      serviceId: client
  sensitive-headers:
  ignored-patterns:
  - /client/env
  - /client/login
